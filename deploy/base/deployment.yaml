# deploy/base/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: bold-rewards-svc-template # Имя будет дополнено префиксом в kustomization.yaml
spec:
  replicas: 1
  selector:
    matchLabels:
      app: my-app
  template:
    metadata:
      labels:
        app: my-app
    spec:
      containers:
      - name: app
        image: nestjs-app:latest # Will be replaced in kustomization
        ports:
        - containerPort: 3000
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "250m"
            memory: "256Mi"
